<Window x:Class="PseudoRun.Desktop.Views.Dialogs.ExamplesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pseudocode Examples" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Filters and Examples List -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Category" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox Grid.Row="1" Name="CategoryComboBox"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      Margin="0,0,0,15"/>

            <TextBlock Grid.Row="2" Text="Search" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Name="SearchTextBox"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,20,0,15"
                     Grid.Row="2"/>

            <ListBox Grid.Row="3" Name="ExamplesListBox"
                     ItemsSource="{Binding FilteredExamples}"
                     SelectedItem="{Binding SelectedExample}"
                     DisplayMemberPath="Title"/>
        </Grid>

        <!-- Right Panel: Example Details -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Name="ExampleTitle"
                       Text="{Binding SelectedExample.Title}"
                       FontSize="18" FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Name="ExampleDescription"
                       Text="{Binding SelectedExample.Description}"
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"
                       FontSize="12"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Difficulty: " FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedExample.Difficulty}"/>
                <TextBlock Text=" | Category: " FontWeight="Bold" Margin="15,0,0,0"/>
                <TextBlock Text="{Binding SelectedExample.Category}"/>
            </StackPanel>

            <Border Grid.Row="3" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" Margin="0,0,0,10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox Name="ExampleCodeTextBox"
                             Text="{Binding SelectedExample.Code}"
                             FontFamily="Consolas"
                             FontSize="12"
                             Background="#F8F8F8"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             BorderThickness="0"
                             Padding="10"/>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="LoadExampleButton"
                        Content="Load to Editor"
                        Command="{Binding LoadExampleCommand}"
                        Width="120"
                        Margin="0,0,10,0"
                        Style="{StaticResource PrimaryButton}"/>
                <Button Name="CloseButton"
                        Content="Close"
                        Click="CloseButton_Click"
                        Width="80"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
